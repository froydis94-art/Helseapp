PORT=8787
# Create a NEW token at https://replicate.com/account/api-tokens
# (revoke any old token that was pasted into Gemini/Word/App.js)
REPLICATE_API_TOKEN=r8_your_new_token_here
# Image edit model — keeps the person in the photo
REPLICATE_MODEL=black-forest-labs/flux-kontext-pro
# Cheaper alternative: black-forest-labs/flux-kontext-dev
