PORT=8787
# Create a NEW token at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_your_new_token_here
# Primary (best identity). May block shirtless physique photos (E005).
REPLICATE_MODEL=black-forest-labs/flux-kontext-pro
# Automatic fallback when Flux safety filter blocks the input/output
REPLICATE_FALLBACK_MODEL=stability-ai/sdxl
